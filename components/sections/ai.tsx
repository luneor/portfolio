import { SectionHeading } from "@/components/section-heading";

export function Ai() {
  return (
    <section id="ai" aria-labelledby="ai-heading" className="py-24">
      <div className="mx-auto max-w-[1120px] px-6">
        <SectionHeading
          eyebrow="Ways of working"
          title="AI"
          headingId="ai-heading"
        >
          {/* Placeholder — real content to come. */}
          <p>This is how I like to work with AI.</p>
        </SectionHeading>
      </div>
    </section>
  );
}
